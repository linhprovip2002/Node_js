<div class="mt-4" style="margin-bottom: 100px;"></div>

<div class="container">
  <h3>Chỉnh sửa thông tin sản phẩm</h3>
  <form method="POST" action="/me/{{product._id}}?_method=PUT">
    <div class="form-group">
      <label for="nameProduct">Tên sản phẩm</label>
      <input type="text" class="form-control" value="{{product.nameProduct}}"id="nameProduct" name="nameProduct">
      <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
    </div>
    <div class="form-group">
      <label for="desciption">Mô tả sản phẩm</label>
      <textarea type="text" class="form-control"  id="desciption" name="desciption">{{product.desciption}}</textarea> 
      <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
    </div>
    <div class="form-group">
      <label for="parent_id">Thể loại</label>
      <input type="text" class="form-control" value="{{product.parent_id}}"id="parent_id" name="parent_id">
      <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
    </div>
    <div class="form-group">
      <label for="videoID">Video ID</label>
      <input type="text" class="form-control" value="{{product.videoID}}"id="videoID" name="videoID">
      <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
    </div>

    <button type="submit" class="btn btn-primary">Lưu lại</button>
  </form>
</div>